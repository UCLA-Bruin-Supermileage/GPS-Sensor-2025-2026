This repo will eventually contain the following: 

An easy interface with NMEA sentences output by the GPS Scanner (found in a decoder script).

An interface with the antennae we wish to send RTCM output and input data to the STM32s.

And some steps to gain access to RTCM data correction data from a base station near us.
